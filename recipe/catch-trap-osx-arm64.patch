--- src/common/catch.hpp	2018-10-17 09:48:15.000000000 -0700
+++ src/common/catch.hpp	2021-10-22 05:25:34.000000000 -0700
@@ -4722,8 +4722,12 @@
 }
 
 #ifdef CATCH_PLATFORM_MAC
-
-    #define CATCH_TRAP() __asm__("int $3\n" : : ) /* NOLINT */
+    #if defined(__aarch64__)
+        #include <signal.h>
+        #define CATCH_TRAP() raise(SIGTRAP)
+    #else
+        #define CATCH_TRAP() __asm__("int $3\n" : : ) /* NOLINT */
+    #endif
 
 #elif defined(CATCH_PLATFORM_LINUX)
     // If we can use inline assembler, do it because this allows us to break
--- src/client/tests/catch.hpp	2020-07-08 11:56:49.000000000 -0700
+++ src/client/tests/catch.hpp	2021-10-22 05:29:17.000000000 -0700
@@ -4723,7 +4723,12 @@
 
 #ifdef CATCH_PLATFORM_MAC
 
-    #define CATCH_TRAP() __asm__("int $3\n" : : ) /* NOLINT */
+    #if defined(__aarch64__)
+        #include <signal.h>
+        #define CATCH_TRAP() raise(SIGTRAP)
+    #else
+        #define CATCH_TRAP() __asm__("int $3\n" : : ) /* NOLINT */
+    #endif
 
 #elif defined(CATCH_PLATFORM_LINUX)
     // If we can use inline assembler, do it because this allows us to break
